# Vision-Grounded Gesture-Sequence Leap — Journal

Tracking the leap from open-loop single-trick CMA-ES to closed-loop, perception-grounded gesture **sequences** (lines, park). Plan: a two-model spine — **Model 1** (learned trace extractor: frame → touch coords, self-labeled by the agent's known touches) feeding **Model 2** (n frames + m gestures → next m gestures, trained on Model-1-labeled expert play). See `/Users/ashernoble/.claude/plans/tender-foraging-creek.md` Part B.

## Data Reality — Verified On Disk (2026-06-14)

Investigated the three candidate data sources. The premise of Asher's idea (reliably extract touch coords from True Skate's orange finger-trace) is **sound but needs the right data + ground truth**:

1. **"True Skate w: my Hand Clips/" (11 clips, ~14 min, 1920×1080) = EXTERNAL CAMERA footage**, filmed over Asher's shoulder — the phone screen is a small, tilted, glare-washed region in the frame. trace_extractor gets **0%** here (no clean full-frame screen to read). **Unusable for trace extraction.** Don't build on these.
2. **`data/extracted_frames/` (6 clips, 727 frames, portrait ~750×1624) = proper SCREEN recordings** — the original BC dataset, the format trace_extractor was built for. True Skate is **portrait** (the landscape hand-clips were just the externally-filmed phone; resolves the orientation confusion — matches the RL device coords 414×896 portrait).
3. **RL self-play frames** are grayscale 210×455 (FrameRecorder does `.convert("L")`) AND discarded (`record_frames=False`) AND captured post-gesture — so no usable color trace corpus exists yet.

## The Orange Finger-Trace Is Real, Distinctive, and Transient

- Confirmed visually: a 360-flip frame (`360_single_flip_001/img_00010.jpg`) shows a bright **orange swoosh** trailing the flick — the finger-trace True Skate renders. It's the signal Model 1 reads.
- **Transient:** present only during/just after an active drag; mid-air/settled frames have no trace (already faded). Labeling must align frames to when the touch is active.

## trace_extractor Reliability — Quantified

Ran the CURRENT `TraceExtractor.process_frame` over each clean clip sequentially (`.reset()` per clip):

| clip | frames | extractor active | median warm-px/frame |
|---|---|---|---|
| 360_single_flip_001 | 95 | **100%** | 9,465 |
| double_kickflip_003 | 91 | **47%** | 4,079 |
| laser_flip_001 | 135 | **95%** | 72,543 |
| gazelle_double_heel_001 | 234 | **100%** | 12,907 |

- The committed `double_kickflip_003_60fps_labels.csv` shows **0%** active — it is **STALE** (old extractor/config); current code gets 47% on the same clip. (So "BC failed on bad labels" is partly a stale-pipeline artifact.)
- **Caveat — the active flag is not the same as positional accuracy.** Warm-pixel floors are huge (laser_flip median 72k px/frame): the warm *board grip + ground reflections* dominate, so the extractor can fire on the board rather than the finger trace. Whether the detected (x,y) is the true touch is **unverified by anything except ground truth.**

## Conclusion → Why Asher's Agent-as-Labeler Is the Right Move

- The trace exists and the extractor *fires*, but its **positional accuracy is unknown** and the hand-tuned HSV+temporal heuristic clearly mislabels in places. There is no ground truth to validate or train against — until we make some.
- **The agent generates touches it knows exactly.** Capture clean screen frames during agent gestures → `(frame, known-touch)` is free ground truth. Use it to (a) **measure** trace_extractor's true positional error, and (b) **train Model 1** (a learned extractor that beats the heuristic). This is the unblocking step for the whole leap.
- **Prerequisite that wasn't obvious:** the corpus must be **color, full-frame screen captures** (like `data/extracted_frames`, or AVFoundation USB capture) — NOT the grayscale 210×455 RL frames and NOT the external hand-clips. The Stage-1 capture must save color screen frames.

## On-Device Self-Labeling — VALIDATED (2026-06-14, iPhone_XR)

Brought WDA+Appium up (directly, no launcher monitor — see ops note) and ran the new `collect_self_labeled_traces.py`. Color MJPEG capture works: full-res **1792×828 RGB** frames with per-frame timestamps. Findings from ~140 collected gestures:

1. **The trace only renders for on-board FLICKS, not arbitrary drags.** Random screen drags in empty space produce no trace and don't move the board (`warm@label=0`). Fixed the sampler to start on the board (~x∈[0.38,0.62], y∈[0.50,0.80]) and flick outward — every such flick produces a trace (50/50). (Some flicks even land real tricks, e.g. a BACKSIDE 180.)
2. **The orange trace LAGS the flick by ~0.4-0.5s.** The swoosh peaks a median **+0.33s after the gesture END** (50/50 samples, range +0.14..+0.66s) — it's a lingering render of the completed flick path, not coincident with the finger. My first instantaneous-point labeling therefore marked the trace-rich frames "inactive."
3. **Latency-shifted labels align with the trace.** Sweeping `latency_s`: at 0.0s only **1%** of active-frame labels land on the trace; at **0.40s → 78%**, at **0.50s → 85%** (median warm-px at label jumps 0 → ~2000). **`latency_s ≈ 0.45` is the validated offset** — now the default in `train_trace_extractor.py` + a trace-presence gate keeps only aligned frames.

**Conclusion: Asher's agent-as-labeler approach WORKS on-device.** Clean color frames + known flicks + a ~0.45s latency shift → a corpus where labels sit on the rendered trace ~80% of the time. The label naturally lands at the flick's end (swoosh head). For Model 2 the richer target is the whole flick *path* the swoosh encodes (refinement). Corpora on disk: `data/self_labeled_traces/iPhone_XR_20260614_011235` (50, board-flick) + `..._011856` (250) — gitignored under /data/.

## MODEL 1 TRAINED + GENERALIZES (2026-06-14) — the leap's perception foundation is proven

- Trained the `GaussianBumpPredictor` U-Net on the 50-sample board-flick corpus (latency 0.45 + trace gate → 123 trace-aligned positives + negatives = 192 frames, 20 epochs, MPS). Loss fell monotonically **0.044 → 0.00096**.
- **Held-out generalization (243 trace frames from the SEPARATE 250-sample session, seed 23 ≠ train seed 11):** predicted touch → ground-truth-label **median normalised distance 0.032** (≈3% of frame), **100% of predictions land on the orange trace** (median warm-px at prediction 3186 ≥ 2703 at the label). Visual overlays (`tmp/model1_pred/`) show pred (red) and label (green) coincident on the swoosh.
- This is the go-signal: a tiny self-labeled corpus already yields a learned trace extractor that accurately reads the finger trace on unseen data. Scaling the corpus (the 250-sample run, more sessions, more devices) → a production Model 1, then run it on expert play to label Model 2's sequence data. Ops/cache caveat: training stdout was block-buffered (run `python -u`).

## Ops Note — WDA Stability
- WDA bring-up via `launch_services.py` FLAPPED hard (iproxy "died unexpectedly" → auto-restart → port 4723 wedged). Root cause: the launcher started a SECOND iproxy on 8100 conflicting with the pre-existing tunnel, and its restart monitor then wedged.
- **Fix that worked: launch WDA + Appium DIRECTLY** (`xcodebuild ... test-without-building` + `appium --port 4723`), reusing the existing iproxy tunnels, with NO launcher monitor. Stayed rock-solid for ~25 min across 340 gestures + a training run. The launcher's auto-restart was the instability, not WDA itself. Fold a "reuse existing iproxy / don't double-start" guard into launch_services.

## Scaling Pass #1 + the Real Bottleneck: DOMAIN GAP, not data quantity (2026-06-14)

- **Spin button coord CONFIRMED.** Asher's reference images + a gridline-localised screenshot put the spin button (camera + circular-arrows, a HOLD control) at **~(0.055, 0.40)** — essentially the configured (0.0604,0.404). So the coord was right; spin's failure is the base gesture/combo (holding spin over a 360-pop-shove → no recognised trick), which is a True Skate domain question for Asher, not a localisation bug.
- **Expert corpus is real and rich:** `Projects/Robotics & hardware/Training_Data` — 12GB, **264 screen recordings** (RPReplay/ScreenRecording, 750×1624 portrait), `Sorted/Flatground/{kickflips,360_flips,...}` deeply trick-labeled. (The "Hand Clips" were website footage.) This is Model 2's fuel.
- **Scaling infra built:** multi-session dataset (point at the parent dir), in-memory frame+heatmap cache (training 120s→~45s/epoch; full-res-per-item was the bottleneck), tunable `--img` res + `--base-channels`, checkpoint metadata. Corpus grown to ~596 board-flick samples on disk (50+250+296).
- **Honest result — naive scaling did NOT beat v1 on expert transfer, AND the test was confounded.** Trained v2 on 300 samples but (to train fast under Asher's *active* laptop GPU contention) shrank it to 288×128 + base_ch=16 — changing res/arch AND data at once. On 3 expert clips v2 landed on-trace 52-88% (conf ~0.56) vs v1's 57-100% (conf 0.67-0.88). So this says "the smaller/lower-res model is weaker," NOT "more data hurts." A clean data-scaling test must hold arch/res fixed.
- **The deeper finding (the real lever):** v1 was trained ONLY in the agent's warehouse park; the expert clips are in OTHER parks (brighter SLS arena). Neither model has seen that domain. **Expert transfer is a DOMAIN-GAP problem, not a data-quantity problem** — more warehouse data won't close it. The right scaling is **domain-matched collection** (run the agent self-labeling in the same parks the expert clips use — needs True Skate park-switching) and/or semi-supervised training on expert frames. Logged here so the next pass targets domain, not just count.
- Ops: heavy U-Net training contends with the user's live laptop use (epochs swung 45s↔4min). Prefer small models / offload, or train when the laptop is idle.

## Park-Switching Works → Domain-Diverse Collection Unblocked (2026-06-14)

- Navigated True Skate's menus via Appium: bottom **SKATEPARKS** tab (≈0.30,0.95) → **All** tab (≈0.10,0.16) → park row. Confirmed switching loads a new park (state persists across Appium sessions). Installed parks: **The Workshop** (warehouse, the agent's default), **The Glass House** (glass arena), **Skatepark: Underpass** (outdoor urban). SLS-arena parks (the expert clips' domain) are NOT installed — store/download only.
- **Self-labeling transfers across parks unchanged:** on-trace yield at latency 0.45 — Glass House **87%**, Underpass **75%** (vs warehouse ~78%). The trace mechanic + latency are park-invariant; only the background changes. So three distinct visual domains are now collectable.
- Added `--park glasshouse|workshop|underpass` to `collect_self_labeled_traces.py` (`switch_to_park()` does the menu nav; row y-positions are position-dependent — re-check if the installed set changes). Output dirs get a `_<park>` tag.
- **This is the corrected scaling lever** (vs scaling pass #1's same-park data): a multi-park corpus → a domain-robust Model 1 that should generalise to UNSEEN parks (incl. the SLS arena the expert clips use). Multi-park collection (Glass House + Underpass, 150 each) running; next: train a fixed-arch Model 1 on balanced multi-park data and re-test transfer to the SLS-arena expert clips.

## SLS Arena (the actual expert domain) Installed + Collected (2026-06-14)

- Asher re-signed the XR's App Store into the Apple ID that owns the SLS DLC and installed his owned parks. The SLS arenas (SLS 2016 Super Crown / Newark / Munich, SLS 2015 Super Crown) are now installed — these are the **exact domain** of the expert clips ("SUPER CROWN / WORLD CHAMPIONSHIP" banners).
- SLS parks live under the SKATEPARKS **SLS tab** (not the All tab), so `switch_to_park` doesn't cover them; added `--tag <label>` to the collector — select the SLS park manually, then `--tag sls_supercrown` labels the session so the tonight runner auto-includes it. (SLS parks themselves are $1.99 DLC / coin parks via "Get more parks"; the coin parks like Love Park/Warehouse/Schoolyard are 200K coins.)
- Collected **174 samples in SLS 2016 Super Crown** (86% on-trace) — domain-matched to the expert clips.
- **Tonight's 2am training corpus is now domain-matched:** Workshop 250 + Glass House 150 + **SLS Super Crown 174**, underpass EXCLUDED. The transfer eval (vs the SLS-arena expert clips) is now an IN-domain test — should beat the warehouse-only v1's partial transfer. If it does, scale SLS collection (Newark/Munich + more Super Crown) and start labeling the 264-clip corpus for Model 2.

## Status / Next
- **DONE this session:** self-labeling pipeline validated end-to-end on-device; Model 1 trained + generalizes; board localizer + reanchor; spin extension (code) — all committed on `feat/spin-and-vision-sequence-leap` (not pushed).
- **Next (supervised):** (1) scale the self-labeled corpus (250+ run on disk; collect on XR2/XS too) and train a production Model 1; (2) run Model 1 on screen-recorded expert play → Model 2 sequence dataset; (3) fix the spin execution (second finger in one W3C payload) + confirm the spin input with Asher; (4) Option-D first 2-trick line using the board localizer + reanchor.

## SLS Sequence-Data Collection + the Clapperboard Latency Result (2026-06-16)

Built the **SLS random-gesture trace collector** (`scripts/data/collect_sls_traces.py`) — the missing half of the sequence-model corpus. CMA-ES trick mining must stay on the single-reset flatground park (obstacles + multi-reset contaminate params), so SLS arenas are used ONLY to collect `(frame, known-gesture)` pairs. It is **obstacle-tolerant**: the executed gesture is the label, so wall-bumps / whiffs / varied board positions are *desirable diversity*, not contamination. Mixture sampler (`data/gesture_sampling.py`): **60% board-flicks / 25% full-random N-slot vectors / 15% perturbed library recipes** (`recipe_to_vector` round-trips exactly through `unpack_gesture_params`). Per-sample meta carries the gesture (label), park, `frame_times`, and the capture offset.

**The clapperboard (frame↔gesture latency) — the load-bearing result.** Goal: Δ, the command→frame pipeline lag, so a frame pairs to the right point in the gesture timeline. The dead-ends and the answer:
- **Reset-snap via the board localizer FAILED.** On a reset the board *vanishes* from the localizer for a variable ~0.1–0.4s (reset animation) then reappears at home ~0.45s later — no crisp onset; board-centroid Δ swung **0.067 → 0.49s** (jitter 0.20). Anchoring to the settled-home position rejected the localizer's spurious mid-transition jumps but yielded too few clean samples.
- **No in-game visual is instant.** True Skate animates everything: the `•••` menu ramps over ~0.3s, SETTINGS over ~1.0s. So the sync event can't be a True Skate element.
- **Solution — a dedicated Clapperboard app** (`github.com/AsherNoble/Clapperboard`, a SwiftUI full-screen black↔white toggle that flips in ONE frame, animations disabled). The rig `activate_app`s it, taps to flip, times the flip in the MJPEG, returns to True Skate.
- **KEY INSIGHT — anchor to the COMMAND CALL-RETURN, not its issue time.** Appium's synchronous tap has ~**0.85s of command overhead BEFORE the effect** (variable ±0.1s), but the flip reliably appears **~0.07s BEFORE the call returns**. Flip-vs-issue jitters ±0.1s; **flip-vs-return is −0.037s ± 0.027s (sub-frame)**. So `Δ = flip_frame_time − command_call_return`, and each sample's `frame_times` are anchored to the gesture call-return (`gesture_end_monotonic`); the on-screen gesture spans `frame_times [Δ − duration, Δ]`. Device-verified: **n=14/14, Δ −0.037s, MAD 0.027s**.
- **Δ is ~constant per session → calibrate once at startup** (not per-eval), which is why the app-switch is affordable. MJPEG ~10–15fps floors per-frame precision at ~0.07–0.1s anyway; our offset jitter is already under that.
- **Apple Dev account:** free signing expires every **7 days** (the weekly WDA re-sign pain); paid **$99/yr → ~1 year**, fixing it for WDA *and* the Clapperboard app.

**Park rotation — rudimentary ntfy tap-to-confirm (vision detector rejected).** A first attempt (a vision diverge-then-resettle `park_change` detector) was brittle and scrapped. Now: on the per-park timer the collector sends a **"switch park" notification with a tappable `http` action button**; tapping "I switched parks" POSTs to a derived ntfy **control topic** (`<topic>_ctl`) that the collector polls; on receipt it advances the park label + resets the timer. It keeps collecting in the current park until you tap (no idle time when away); a plain "done"/"switched" message typed to the topic also works. Device-verified end-to-end on the iPhone 11 (samples re-tagged to the new park after one tap). Gotcha logged: an emoji in an HTTP header (latin-1) broke the whole notification — `notify` now sanitizes header values.

**Also (slop-review pass, now on this branch):** trick-library spin REPLAY was unified onto the canonical eval path — `sim/gesture_recipe.execute_gesture_recipe` now delegates to `execute_static_push` + `execute_n_slot_gestures` (spin = a HELD finger in one W3C payload), replacing a background-thread `mobile:tap` that cancelled the in-flight gesture and nullified the trick. Device-verified (`branch=combined-spin`). `execute_trick.py` now passes `spin_button_xy` (was a silent no-op).

- **Status:** the SLS collection pipeline + per-session clapperboard + ntfy park-rotation are **device-verified and committed** on `feat/spin-and-vision-sequence-leap`. Next: a real multi-hour SLS run across the 11 parks (start each phone at a staggered `--start-park`), then fold the SLS corpus + flatground TRACE_COLLECT corpus into the production Model 1 retrain, then Model 2.

## Capture Frame-Rate: WDA MJPEG vs AVFoundation/DAL — the 30fps Wall (2026-06-22)
- **Why this matters:** inference-time screen recordings are **60fps**; the SLS corpus is captured via WDA's MJPEG at **~10–15fps, variable**. That's a train/inference *distribution* gap (per-frame motion magnitude, blur, trajectory smoothness all differ for a sequence model), not just a label-precision issue. Also the label floor = 1/fps, so low fps dominates the gesture↔frame error budget (clapperboard jitter MAD 27ms already sits under it).
- **WDA MJPEG is a dead end for 60fps** — it's screenshot-re-encode on the device CPU (A12), inherently variable; bumping `mjpegServerFramerate` would get ~15–25 variable fps at best. Not pursued.
- **Prototyped the AVFoundation/CoreMediaIO DAL path** (the `scripts/view_device.py` front-end — same H.264-over-USB pipeline QuickTime uses; ffmpeg sees the phone only via the `tools/enable_dal.c` DYLD shim). Measured on **iPhone_XR (Test XR_1)**, ffmpeg `-f null` frame counts over 10–15s:
  - Default mode = **1920×1440@30** (4:3) → steady **30.0 fps** (450 frames / 15.0s).
  - Device *advertises* 60fps modes, but only at 16:9 (640×480 / 1280×720 / 1920×1080); the 4:3 max-res mode is 30-only.
  - Requested 60 at **1920×1080 → 30.0 fps** (450/15s); at **1280×720 → 30.0 fps** (300/10s). `speed=1x`, identical across resolutions ⇒ **not** a Mac-side/bandwidth bottleneck. **The DAL screen-mirror stream is hard-capped at 30fps on this XR** — the advertised 60fps formats are camera descriptors the mirror pipeline doesn't actually fill.
- **The one real win:** DAL is **rock-steady 30.0** (frame counts exactly linear) vs MJPEG's variable ~10–15. So DAL fixes the *variability* but delivers *half* the target rate. Still short of 60fps inference.
- **Operational gotchas (carry into any DAL integration):**
  - **AVFoundation device indices REORDER between calls** (Test XR_1 was `[2]` then `[1]`; FaceTime/phones shuffle). Addressing by index opens the WRONG device → **always reference by exact NAME** (`-i "Test XR_1"`).
  - DAL device is **exclusive + fragile**: repeated open/close cycling produced intermittent then persistent `Input/output error` (the documented CMIO wedge, memory `ios-dal-screen-capture-wedge`). Fix = QuickTime New Movie Recording re-activate, or replug; CMIO/usbmuxd restart does NOT help.
  - Capture modes are 4:3 / 16:9, **none match the phone's ~2.16:1 screen** → a DAL frame letterboxes/pillarboxes the game, a *framing* mismatch vs native screen recordings (favors on-device capture for distribution match). [unverified by eyeball — frame grab failed on the wedge]
- **Decision pending (Asher):** B = ship DAL at **steady 30fps** (accept half-rate, gain steadiness + likely temporally-interpolate to 60 offline), vs C = **on-device ReplayKit 60fps recording** + offline alignment via an in-frame timecode (true 60fps + native framing, but loses the live monotonic-clock sync — more engineering). **60fps is only reachable via C on this hardware.**
- **Drag-vs-tap Δ check is DEFERRED** until the capture path is chosen — it must run on the pipeline we actually ship (its latency Δ is pipeline-specific), not the about-to-change MJPEG path.

## DAL Capture Built — But the Clapperboard Is Invisible to It (2026-06-22, on-device)
- **Decision taken (Asher): Option B — DAL at steady 30fps.** Built the capture subsystem: `vision/dal_capture.py` (`DalFrameRecorder`, a ONE persistent `ffmpeg avfoundation→mjpeg` stream + reader thread timestamping each frame into a rolling buffer; `window(t0,t1)` slices per sample — NOT start/stop per sample, which wedges the exclusive device). Refactored `view_device.py` onto the shared DAL shim env (one source of truth). Added `avf_name` to DEVICES (`iPhone_XR→"Test XR_1"`, `iPhone_XR2→"Test_ XR_2"`; address BY NAME — AVFoundation indices reorder). `clapperboard.calibrate_via_app` now takes an injected recorder + a pluggable `action_fn`; collector window-slices the DAL buffer + self-heals stalls + always `close()`s. All offline-verified; clapperboard Δ self-test still passes.
- **DAL framing is FINE (earlier letterbox worry retracted):** a real DAL frame is the clean NATIVE portrait screen of True Skate, full-frame, no bars (`tmp/dal_probe2.png`). Games capture cleanly.
- **BLOCKER 1 — DAL renders the Clapperboard app PURE BLACK (lum 0.0).** DAL captures True Skate (Metal game) perfectly, but the SwiftUI Clapperboard mirrors as all-black under AVFoundation/DAL (frames flow ~30/s, alive, but every pixel 0.0). WDA's XCTest screenshot of the SAME app sees the flip fine — **mean luminance 177.7 ↔ 1.0 per tap**. So the app works; the AVFoundation screen-capture path just can't see it. **⇒ the clapperboard Δ calibration cannot run over DAL.**
- **BLOCKER 2 — the Clapperboard flips on a clean TAP only, not a drag.** Drag conditions gave n=0 flips every batch (a moving `curved_drag` isn't recognized as a tap; it's also not touch-down/button-triggered, or the drag would've fired it). So **Δ_drag is unmeasurable with this app** — the drag-vs-tap empirical check needs the app to respond to gestures (add a `DragGesture`/down-trigger), or we reason analytically.
- **Partial result — Δ_tap over WDA MJPEG = +0.023s, MAD 9ms (n=40).** Tight and sub-2-frames-at-30fps. (Sign/magnitude differ from the earlier −0.037s; pipeline/threshold/device-dependent — the point is it's small + consistent.) The drag-vs-tap *difference* is a WDA-command property and transfers across pipelines, so it could be measured over MJPEG IF the app responded to drags.
- **Open decision — how to get Δ for the DAL pipeline** (none need 60fps): (A) make the Clapperboard DAL-visible + drag-responsive (Asher's iOS app fix + re-sign — best, unblocks both); (B) reset-snap Δ over DAL via the board localizer (True Skate is DAL-visible; journal-noted noisy); (C) ship offset=null / a fixed constant (single-frame Model-1 tolerates a ~1-frame call-return-anchor bias); (D) cross-pipeline: Δ_DAL = Δ_MJPEG(clapperboard, works) + (DAL−MJPEG capture-latency gap measured on a shared True-Skate reset event — automatable, precise, no app change).
- **Bundle-id fix:** the rig's Clapperboard is `com.ashernoble.clapperboard` (re-signed under Asher's team), not the stale `com.embodiedstates.*` default — updated `DEFAULT_CLAPPERBOARD_BUNDLE`. Both XRs confirmed via `xcrun devicectl device info apps`. (`ideviceinstaller` isn't installed; devicectl works.)

## DAL FREEZES Silently — the Scary Failure Mode (2026-06-23, ~1am, chosen path D then bed)
- **Δ_MJPEG measured (clapperboard over MJPEG): +0.034s, MAD 0.016s, n=12** — consistent with the drag-vs-tap tap value (+0.023s). Small positive, ~1 frame at 30fps. (The MJPEG pipeline is fine; this is the cross-pipeline base.)
- **Cross-pipeline gap G BLOCKED — the DAL stream FROZE.** Over 10 board resets: MJPEG saw the snap clearly every time (**~13–15% of pixels change, onset ~0.31s, peak ~0.62s**), but **DAL showed 0.0% change across 43–45 frames** — every frame bitwise-identical. The reset is real (MJPEG proves it); DAL was delivering frozen duplicates.
- **The dangerous part — the freeze is SILENT to a naive watchdog.** When the CMIO/DAL session stalls, ffmpeg's `-vf fps=30` pads the output with bitwise-identical duplicate frames. So `frame_count` keeps climbing, `last_frame_age` stays ~0, `is_alive()` stays True — yet the content is frozen. A collector trusting frame-arrival would **silently save frozen frames labelled with gestures → a corrupted corpus**. A *fresh* ffmpeg open shows live content (mean_lum 92.8), so it's the persistent session that wedges, almost certainly triggered by the night's app-switching (True Skate ↔ Clapperboard for calibration). Same CMIO wedge family as memory `ios-dal-screen-capture-wedge`.
- **FIX (made, UNTESTED on-device — DAL wedged before I could verify):** `DalFrameRecorder` now tracks `content_stale_age` (subsampled exact-equality → time since decoded content last *changed*). Collector: (1) startup **liveness check** — force a push+reset, confirm content moved, else fail with the wedge hint; (2) in-loop health check restarts on `content_stale_age>8s` then forces a reset and confirms LIVE, else STOPS (a restart can't clear a CMIO wedge, so never collect frozen). Added `--capture-offset-s` to stamp Δ_DAL directly while the DAL clapperboard is blind.
- **Drag-vs-tap — analytical answer (empirical deferred to tomorrow's app fix):** both `mobile: tap` and the W3C drag `perform()` are synchronous and return after the FINAL HID event (the lift) is flushed, so the tap's Δ should anchor the gesture's ENDPOINT (lift) the same way → **LIFT-anchored**. Mid-path positions ride the separate execution-timing-fidelity term (nominal duration/easing on-device), not Δ. Fully **retroactively correctable**: every sample stores raw `frame_times` + the offset, so any Δ revision tomorrow is an offline relabel, no re-collection.
- **TOMORROW (Asher):** (1) clear the DAL wedge (QuickTime > New Movie Recording > pick device > quit); (2) fix the Clapperboard app — render DAL-visible (it mirrors BLACK now) + add a DragGesture (flips on tap only now); (3) **verify DAL stays LIVE through a pure-collection run (no app-switch)** — the freeze may be app-switch-triggered, in which case B is viable for collection and only calibration is affected; (4) then DAL clapperboard Δ works directly (retire cross-pipeline) and the real drag-vs-tap check runs. Code is on disk, UNCOMMITTED.

## DAL-Black ROOT CAUSE = app-switching, NOT sessions/the app (2026-06-23) — Option B lives
- Yesterday's "DAL renders the Clapperboard black" was MISATTRIBUTED. Controlled tests today (on XR2; XR1 kept dropping off usbmuxd):
  - DAL with no automation → LIVE (lum ~94).
  - DAL with WDA running AND an active Appium/XCUITest session, **no app-switch** → **LIVE (lum 93 ×3)** (`tmp/session_dal_test.py`). Neither WDA-running nor an active session blacks DAL — matches `view_device`'s coexistence claim.
  - DAL through an app-switch (True Skate ↔ Clapperboard) → BLACK/frozen, and STAYS frozen after switching back.
- **Conclusion: APP-SWITCHING is the CMIO-wedge trigger; sessions/WDA are fine.** Therefore:
  - **DAL collection is VIABLE** — the collect loop never app-switches (stays in True Skate), so DAL stays live while WDA drives gestures.
  - The Clapperboard's "DAL invisibility" is a NON-ISSUE — we never capture it via DAL. **Δ_DAL = cross-pipeline**: Δ_MJPEG (Clapperboard over MJPEG — app-switch is fine, MJPEG survives it) + the DAL−MJPEG gap (over True-Skate resets — no switch, DAL live). No app rendering fix needed; the drag fix stands for the drag-vs-tap-over-MJPEG check.
  - The collector must NOT Clapperboard-calibrate in-process (the switch wedges the DAL it just opened, persistently). Use `--capture-offset-s` with a pre-measured Δ_DAL.
- **Yesterday's cross-pipeline gap failed only on ORDER:** it ran Δ_MJPEG (Clapperboard, app-switch → froze DAL) BEFORE the gap (DAL frozen → 0 motion). Fix: gap FIRST (DAL live), then the Clapperboard. (cross_pipeline_delta.py to be reordered.)
- **Live obstacle = USB reliability, not capture.** XR1 then XR2 repeatedly dropped off usbmuxd (lockdown "could not find device"; gestures socket-hang-up) while CoreMediaIO/DAL stayed fine. Asher's new USB data hub (≈2026-06-24) should stabilize it. **Deferred to then:** reorder + run cross_pipeline to get Δ_DAL, rewire the collector calibration (no in-process Clapperboard under DAL), and a pure-collection DAL-liveness soak.

## Cross-pipeline Δ_DAL measured on XR1 — works; clapperboard leg is the weak link (2026-06-24)
New USB hub in; both XRs on it. Ran `cross_pipeline_delta.py` on **iPhone_XR (Test XR_1)**. Two real bugs fixed before any number came out, then a clean result, then I re-wedged DAL probing a shortcut.
- **BUG 1 (also bites the collector): `resolve_device_name("Test XR_1")` returned `"Test XR_1 Camera"`** — the AVFoundation list has BOTH the bare screen-mirror `Test XR_1` AND a Continuity-Camera `Test XR_1 Camera` (Camera listed FIRST), and the first-substring-match grabbed the lens, not the screen. Fix (`dal_capture.py`): prefer an exact case-insensitive match, else skip the `… Camera` variant. The old docstring's "substring → `…Camera` is a valid target" assumption was just wrong.
- **BUG 2: `motion_onset` missed the reset/push in one or both pipelines.** Causes: (a) measured cumulative change vs each pipeline's OWN frame-0, but the two frame-0s land at different physical times/board-positions; (b) the adaptive `max(early)*2` threshold was computed from frames already mid-motion (MJPEG is **change-driven** — ~7fps static, ~18fps moving — so the 0.3s "baseline" frames fell into the event); (c) DAL has a **slow self-noise drift up to ~0.04 vs frame-0** while MJPEG static ≈ 0.000, so any absolute threshold biases the gap. Redesign: settle to ACTUAL rest via `wait_for_quiet` (DAL frame-to-frame quiet, not a fixed sleep); measure the **PUSH** as the shared event (reset FIRST to stop+recenter — a pushed board never goes quiet); threshold = **median(pre-event frac)+0.04 PER PIPELINE** (cancels DAL's higher floor); sub-frame linear interpolation of the crossing; only search at/after `t_event−guard`. Added `--warmup` (first ~4 samples ran a ~-0.097 regime vs steady ~-0.055).
- **RESULT (clean 20/20 run, pre-wedge): DAL−MJPEG gap = −0.0554s, MAD 8ms.** DAL LEADS MJPEG by ~55ms (hardware-USB path is lower-latency than WDA's XCTest MJPEG — physically sensible). Frame floor stays 33ms (steady 30fps).
- **WEAK LINK = Δ_MJPEG (clapperboard).** Wandered run-to-run this session (+0.012 → +0.037s; MAD up to 30ms; a post-wedge run even read −0.035). The module doc expects ~−0.073±0.015. So Δ_DAL = Δ_MJPEG + gap ≈ **−0.04s but only ±~20–30ms** — the gap is solid, the clapperboard offset is not. Suspect the app's drag-fix (flip on finger-UP via DragGesture.onEnded) shifted the tap-flip timing. **Validate the clapperboard leg before trusting an absolute Δ_DAL.** (Margin-of-error answer for finger-per-frame: ≈ frame quant ±16ms ⊕ Δ_DAL ±25ms ≈ ~1 frame at 30fps.)
- **Direct-DAL-clapperboard shortcut DISPROVEN:** opening a FRESH DAL stream AFTER switching to Clapperboard still gets **zero frames** — the app-switch wedges the DEVICE, not just a pre-existing session. Cross-pipeline is genuinely required; no app render-fix would help.
- **WEDGE NUANCE (refines the recovery picture):** post-app-switch, the wedge is NOT uniformly below ffmpeg. The `-framerate 30 -i` path (what `DalFrameRecorder` + the collector use, and what gives steady 30fps) yields **pure black (lum 0.0)**; a no-`-framerate` open yields **LIVE (lum 94)** but at an unusable duplicated rate; `fps=30` filter without input `-framerate` stalls (~1 frame). **Standalone `-frames:v 1` probes are MISLEADING** — they catch a live frame while the persistent stream is black. So neither a single-probe nor a frame-arrival check detects this; must check the PERSISTENT stream's content/luminance. Clean recovery still = QuickTime New Movie Recording re-activate.
- **Self-inflicted:** the direct-shortcut test (the app-switch) re-wedged DAL AND the Clapperboard `activate_app` round-trip dropped True Skate out of skate mode — MJPEG (independent of DAL) then showed 0 push-motion, i.e. pushes hit hub chrome. Recovery needs BOTH: QuickTime re-activate (DAL) + re-enter the active skate view. Lesson reinforced: do not app-switch once a DAL run is in play.
- Code (resolver fix + onset redesign + warmup) on `feat/dal-capture-prep`, UNCOMMITTED.

## Model 2 Scaffold Built + Verified — the sequence policy + labeler spine (2026-06-27)
New pure-torch package `src/trueskate_ai/bc/` (NO Appium/pyobjc — containerizes for cloud; the portability target, see `~/.claude/plans/start-investigating-how-we-jazzy-stroustrup.md`). All four pieces built and self-verified on MPS:
- **`gesture_tokens.py`** — the atomic action = one **stroke** (`[x0,y0,x1,y1,x2,y2,dur,easing,delay_before]`, STROKE_DIM=9; a curved drag + inter-stroke delay). Bounds sourced from `action_param` (one slot + the delay row → single source of truth). `encode/decode` ↔ normalised [0,1] round-trips to ~1e-16; `strokes_to_param_vector` packs a stroke run to the `9N-1` CMA vector so a predicted chunk executes via `execute_gesture_params`.
- **`model2.py`** — `SequencePolicy`: CNN frame encoder + Transformer set-encoder with a readout **query** token (decision-transformer style) → next stroke-chunk in [0,1]. **3.62M params** @ d=256. Verified it LEARNS (synthetic target = mean of past strokes → loss 0.024→0.006 over 15 epochs, so it genuinely attends the stroke tokens). Readout (not causal decoder) is enough for v1 since the action is a compact macro + we deploy receding-horizon; CVAE/diffusion head is the documented upgrade if mode-averaging shows.
- **`assemble.py`** — the **inverse of `self_label`**: per-frame touch track → segment active runs (gap-bridged) → fit one stroke each (3 waypoints via max chord-deviation control pt; duration; easing via `s≈τ^p` log-log fit). Round-trip vs `self_label` recovers waypoints, duration, easing≈1.0, and inter-stroke delay. This is the "+gesture-assembly" half of the "frame-seq→gesture" labeler.
- **`sequence_dataset.py`** — reads clip dirs (`frame_*.png` + `clip.json{fps,strokes}`) → CAUSAL `(frames[t−n:t], past[t−m:t]) → next[t:t+m_out]` tuples (encoded, front-padded + masked). Verified end-to-end on a synthetic clip: correct shapes + `train --data` runs, loss falls, ckpt saved.
- **`scripts/train/train_sequence_model.py`** — `--smoke` (synthetic, proves pipeline) + `--data`; checkpoint = `state_dict`+config (portable). Only NEW files added; no existing code touched.
- **Open / next (gated on Phase 1):** real `clip.json` comes from running a **domain-robust Model 1** over the 31GB expert corpus `~/Projects/Robotics & hardware/Training_Data` (`Sorted/`+`Untrimmed/`) then `assemble_strokes`. The warehouse→SLS DOMAIN GAP (this journal) is the blocker, not the scaffold. Cloud-training/portability plan (Modal-first) is the other open thread.

## Model 1 Dataset Adapted for Mixed/New-Format Corpora + Local Smoke (2026-06-30)
Stopped rig collection (both XR phones off) and ran the Model 1 local smoke on EXISTING local legacy data — no Modal, no rig transfer (the laptop already holds both label conventions). `SelfLabeledTraceDataset` (`train_trace_extractor.py`) fixed:
- **rglob** — handles `<session>/<park>/sample_*` nesting (SLS/XCTest), not just the flat `self_labeled` layout.
- **Skips non-flick samples** (params/nslot/recipe) — single-touch Model 1 trains on flicks only (no more `meta["waypoints"]` KeyError on the mixed corpus).
- **`_start_relative_frame_times`** normalizes the convention: `self_labeled` = gesture-START-relative; SLS/XCTest = gesture-END-relative → `fe + duration`.
- **Regression OK:** proven `self_labeled` session → **450 on-trace positives**, trains (loss 0.048), unchanged.
- **New-format ingest OK:** mixed `sls_traces` (57 flick + 42 params, 3-level, end-rel) → 42 skipped, nested found, no crash, trains.
- **Per-path latency = the Δ item, concretely:** on-trace alignment peaks at DIFFERENT offsets per capture path — `self_labeled` (MJPEG, start-rel) **0.45s ≈80%**; legacy SLS (DAL, end-rel) **~0.0s, 48%** (sweep: 0.0→31/65, 0.1→6, ≥0.2→~0, 0.8→3). So `--latency-s` is per-corpus; the real **XCTest** corpus needs its own offset calibrated (clapperboard/sweep) before labels are clean — gates label QUALITY, not pipeline function. Code uncommitted.

## Three BC Bridges Built + Aspect-Ratio Fix (2026-07-01)
Closed the two pipeline gaps flanking the Model 2 scaffold, fixed a frame-geometry bug, and hit one deploy-only MPS bug. All verified by smoke; expert corpus + domain gap still gate real data/quality.
- **Aspect ratio (bug):** Model 2 fed **96×96 square** frames — squished the ~2.16:1 screen, distorting board/trace geometry. Now **208×96 (2.167:1)**, within 0.1% of device 896/414; `FrameEncoder` ends in `AdaptiveAvgPool2d(1)` so H≠W needs no arch change, params unchanged (3.62M). CLI `--img-h/--img-w`.
- **Bridge D+E (`scripts/data/build_bc_clips.py`):** runs trained Model 1 over an expert clip's frames → per-frame `(active,x,y)` (heatmap argmax + peak≥thresh) → `assemble_strokes` → writes SequenceDataset `clip.json` (+ `frame_%06d.png` symlinks, no corpus duplication). `--smoke` synthesizes a known 2-stroke clip and **round-trips it through `SequenceDataset`** (correct `6×3×208×96` tuples). This is the join the scaffold lacked — Model 1 now feeds Model 2.
- **Bridge H (`bc/infer.py` + `scripts/inspect/run_sequence_policy.py`):** pure-torch `SequencePolicyRunner` (rolling frame/stroke buffers, front-pad+mask exactly as training) → receding-horizon device loop (screenshot→observe→act→`execute_gesture_params`→commit). `--dry-run` proves the whole inference→param-vector path with synthetic frames, no device.
- **`delay_before` fix:** `strokes_to_param_vector` packs only the N−1 *between* delays, so the first stroke's predicted `delay_before` was dropped. Runner surfaces it as **`pre_delay_s`** the deploy loop waits out — predicted timing now survives end-to-end (critical at `m_out=1`, where the vector carries no delays at all).
- **MPS deploy bug:** `TransformerEncoder` in `eval()` with a key-padding mask takes a nested-tensor fast path (`aten::_nested_tensor_from_mask_left_aligned`) **unimplemented on MPS** — training (`.train()`) never hit it, inference crashed. Fixed with `enable_nested_tensor=False` (numerically identical; removes a train/eval divergence). Only surfaced by actually running the deploy path.

## OPS MISTAKE — left KeepAlive collectors armed during a blocked bring-up (2026-07-07)
Deploying the widened flick sampler to the rig, I `launchctl bootstrap`ed `com.trueskate.collect.xr1/xr2` (KeepAlive=true) BEFORE the stack was actually working — WDA was dead (Xcode 15.4 can't run the phones' iOS 18.7; separate signing saga too). The failing collectors then sat **retrying silently for days of wall-clock**. When Asher later dropped Xcode 16.2 into `/Applications` (xcodebuild resolves via `/Applications/Xcode.app` → instantly 16.2, no `xcode-select` needed), WDA launched, and on the collectors' very next KeepAlive retry they **auto-started collecting ~57 min unbidden** (2×2.9–3.0 GB, 28 segments each). Asher had NOT asked to start collection. Stopped (bootout, tunnel up so no wedge) and discarded ONLY the 3 `*_20260706_*` session dirs (~5.9 GB); 151 historical dirs untouched.
- **Lesson:** NEVER `bootstrap`/load a KeepAlive collector (or any KeepAlive job that acts on the world) until the whole stack is verified end-to-end AND the user has explicitly green-lit collection. A KeepAlive job left loaded during a blocked bring-up is a landmine: it fires the instant the blocker clears, with no human in the loop.
- **How to apply:** during bring-up, bootstrap only passive deps (services); keep collectors/watchdogs UNLOADED. Load them last, deliberately, on explicit go-ahead. If a bring-up stalls, `bootout` anything already loaded before walking away. Silver lining: the unbidden run proved Xcode 16.2 fixes the iOS-18 WDA launch — WDA came up, collectors connected, 28 seg/phone recorded.

## Model 2 causal decision timeline aligned (2026-07-17)
- Training decisions now match deployment: cold start at clip time 0, then immediately after the previous executable action group completes. Targets retain the wait before the next group; overlap-connected strokes retain negative internal delays and execute in one payload.
- Removed frame-zero fallback. Current-time frames remain causal (`<=`); mid-action cold starts and decisions without an at-or-before frame are excluded with counts. Oversized overlap groups fail with clip/group identity.
- `m_out` is now maximum group size. Model 2 predicts padded strokes plus a supervised active-prefix head; regression ignores padded slots. Checkpoints are v2-only and old Model 2 checkpoints require retraining.
- Deployment now owns a bounded timestamped WDA-MJPEG buffer and replaces each decision's input with a resampled recent ~0.2s window. No live frame means defer/abort, never synthetic context.

## Model 1 causal sequence dataset implemented (2026-07-18)
- Added a sample-preserving temporal loader: flick, spin-flick, n-slot, recipe, and spin metadata decode into stable multi-touch tracks, including negative-delay overlap and XCTest's configured 0.12s finger-down stagger. End-relative capture times are converted to payload-start-relative before the tunable latency shift.
- Every item is one fixed-length, end-padded gesture sequence with RGB, max-combined multi-peak heatmaps, exact centers/counts, frame deltas, reset/valid/label masks. Trace-gate failures remain recurrent context but are unsupervised; no frame is dropped or reordered.
- Bounded Super Crown discovery spreads deterministically across session/park roots without entering frame directories. Optional first/middle/last menu/editor detection covers unflagged legacy contamination. Modal mode can cache preprocessed RGB as uint8 and reports the byte footprint, avoiding FUSE rereads each epoch.
- Verified with 10 focused dataset tests plus the full temporal trainer smoke (scheduled rollout, balanced loss, autoregressive validation, v3 checkpoint).

## Temporal Model 1 + spin-inclusive Modal training (2026-07-18)
- Model 1 is now causal/recurrent: current RGB, previous predicted heatmap, frame delta, and ConvGRU state produce the current multi-peak heatmap plus touch activity. Training uses scheduled teacher feedback; validation is fully autoregressive.
- Balanced heatmap/activity loss is AMP-safe in float32. Strict 90% acceptance now requires touch recall, negative-frame specificity, and peak precision all >=90%; extra false touch peaks can no longer pass. V3 checkpoints include timing/acceptance semantics and exact train/validation split fingerprints.
- Model 1 -> Model 2 clip construction preserves multiple simultaneous peaks with causal tracking instead of one global argmax. Temporal checkpoints run statefully across each clip and old single-frame checkpoints remain explicitly separate.
- Modal corpus startup now overlaps 16 bounded FUSE cache workers; corrupt/non-object metadata is skipped and counted, while malformed supported gesture semantics still fail loudly. Combined local verification: 70 tests, compile, shell syntax, and plist lint pass; patched Modal CPU smoke passes.
- Both XRs are collecting only SLS 2015 Super Crown with `--spin-frac 0.8`. Recent manifests prove real `spin` and `spin_flick` samples; 180 spin-active sequences were priority-uploaded. The deterministic 2,000-sample Modal audit selects 106 spin-active sequences (52 spin-flick, 54 spin).
- Modal inode use is already ~429% of the nominal quota. Auto-offload now sets `MIN_SPIN_FRAC=0.8`: every segment manifest must prove the threshold; missing, malformed, mixed, or pre-spin sessions stay local and are rechecked before any delete. Known 0.8/0.0 rig probes accept/reject correctly.
- Fresh detached A10G run: `ap-I0SQQU0SO4rTfCpZDYL0Ec` (40 epochs, max 2,000 sequences, batch 8, 16 cache workers). Dataset/epoch metrics pending.

## Temporal Model 1 baseline diagnosed + hard-negative correction launched (2026-07-18)
- Baseline `ap-I0SQQU0SO4rTfCpZDYL0Ec` completed 40 epochs. Best checkpoint: `trace_extractor_temporal_v1_20260718_103855_lat0.2.pth`, epoch 38, strict score **6.21%** = touch recall **42.25%**, raw-negative specificity **68.08%**, peak precision **6.21%** (139/329 target touches matched; 2,240 emitted peaks). It was not near the 90% goal.
- Root cause: global background BCE averaged over ~7.1M pixels/batch makes a few high false maxima effectively free, while the downstream metric charges every maximum. Added per-labelled-frame top-K hard-negative focal BCE (target bump excluded), AMP-safe and zero-default. Trainer logs the component and exposes weight/K.
- Validation now performs one fully autoregressive rollout and calibrates peak/activity thresholds over a grid without rerunning the model. Checkpoints persist the chosen decoder thresholds, NMS radius, and max touches; `build_bc_clips.py` resolves CLI -> checkpoint -> historical defaults.
- Training and deployment now share canonical plateau-collapse + greedy-NMS peak decoding and inclusive `(width-1,height-1)` coordinates. Strict raw-heatmap specificity remains the hard gate; activity-only and emitted-output specificities are logged separately.
- Corrected Modal smoke passed and all **93 tests** pass. Detached corrective A10G run: `ap-47c0pDi5n5Oum4igimUTTC`, 30 epochs, hard-negative weight 0.10/top-16, teacher forcing 0.75 -> 0 by epoch 10, no discrete-score LR halving. Dataset: 1,185 retained Super Crown sequences, 155 spin/spin-flick (13.1%), 3 max simultaneous touches.
- Corrective run best checkpoint: `trace_extractor_temporal_v1_20260718_113041_lat0.2.pth`, epoch 27, strict **28.53%** = recall 28.53%, specificity 91.83%, precision 40.71%; raw heatmap specificity 98.39%. This is 4.6x the baseline but not the 90% goal. Multi-touch recall remained 5% (1/20), so overlap quality is still explicitly unresolved.
- Fresh-optimizer fine-tune `ap-EslafhDIGpGUYX3WcBSOhJ`: fully autoregressive, LR 2e-4 -> 2e-5 cosine, hard-negative weight 0.05, heatmap/activity weights 2.0/0.5. Weights-only initialization intentionally resets optimizer, epoch, best score, and split identity so new 0.8-spin uploads may enter without carrying stale validation state.
- Fine-tune best checkpoint: `trace_extractor_temporal_v1_20260718_121330_lat0.2.pth`, epoch 6, strict **32.13%** = recall 32.13%, specificity 88.34%, precision 63.04%; raw/emitted specificity 98.32%. Multi-touch recall improved to 15% (3/20; F1 26.09%) but remains data-starved. Loss-component logging showed spatial/activity contributions are now balanced; the next refinement is positive-heatmap weighting plus genuine-overlap sequence oversampling, not more blind epochs.

## Temporal Model 1 overlap/positive rebalance ready (2026-07-18)
- Added configurable heatmap/activity positive-class loss shares (neutral default 0.5). The next controlled arm uses **0.70 heatmap-positive mass** to lift true peaks while retaining the existing hard-negative term.
- Dataset now reports genuine labeled `>=2`-touch frames per sequence. Train/validation splitting is deterministic and overlap-stratified at the whole-gesture level; split fingerprints are upgraded to `sha256_split_v2` and include overlap counts.
- The sampler reserves **25%** of draw probability for sequences in proportion to their real overlap frames, with the remaining 75% preserving the positive/negative class mixture. No synthesized overlaps or relaxed labels; corpora with no overlap retain the exact previous sampler weights.
- Exact local path verified: temporal smoke saves the 0.70/0.25 config; full suite **103 passed**, compilation and diff checks clean. Modal smoke/paid launch is blocked by the Codex execution quota until **2026-07-25 14:49**; code is launch-ready.
- First queued arm: weights-only init from `trace_extractor_temporal_v1_20260718_121330_lat0.2.pth`, 16/32 channels, max 3,000 Super Crown sequences, fully autoregressive, LR `1e-4 -> 1e-5` cosine, hard-negative 0.05/top-16, heat/activity weights 2.0/0.5, heat-positive 0.70, overlap sampling 0.25. This isolates the data/loss correction.
- Capacity follow-up if that plateaus: fresh 24/48-channel, two-stage model (579,218 params vs 258,018); keep 288x128. Do not add a count head yet—the current validation split has only 10 multi-touch frames.

## Overlap/positive rebalance arm RAN — did NOT beat prior best; recall is the wall (2026-07-19)
- Continued Codex's queued arm (Codex was blocked on its OWN OpenAI quota, not Modal). Verified launch-ready first: **103 tests pass**, Modal CPU smoke passes. Every flag cross-checked against the Eslaf resume checkpoint's saved `training_config` so the arm holds Eslaf constant EXCEPT the 3 intended knobs.
- Ran `ap-KjnfEpNw0AYJMHAF0aSnSo` (A10G, 40 epochs): weights-only init from `..._121330` (Eslaf, epoch-6/32.13%), LR 2e-4→**1e-4** cosine, heatmap-positive **0.70**, overlap-sampling **0.25**; fully autoregressive, heat/act 2.0/0.5, hard-neg 0.05/top-16, 16/32ch, batch 8. Confirmed genuine weights-only resume (lr started 1.00e-04 = fresh cosine, "using checkpoint weights as fresh initialization" logged).
- Corpus is richer now (new 0.8-spin uploads): **1777 retained** (vs Eslaf's 1185), **63 multi-touch sequences / 146 multi-touch frames** (vs ~10), 214 spin-family (12%), max 3 touches. Split 1510 train / 267 val (549 positive val frames). **Note: 8203 of 11503 candidate touch-frames were TRACE-GATED (71%)** — trace not visible at the label.
- RESULT: best `..._133147` **epoch 3, strict 31.51%** (recall 31.51%, spec 84.01%, peak-prec 59.86%). Did NOT beat 32.13% (different/larger val split, not directly comparable). val_accept peaked epoch 3 then DECLINED to 21–24% by epoch 40 — same peak-early-then-fade as Eslaf. **The 0.70/0.25 loss+sampling change did not move the needle.**
- DIAGNOSIS — binding constraint = **RECALL**. strict = min(recall, spec, peak_prec); recall ~31% is the wall (spec 84–92%, prec ~60%). Recall barely moved over 40 epochs (28.6→31.5).
  - **Verified in code (`temporal_trace_training._frame_mask` line 72): the recall metric ANDs `label_mask`** → gated (trace-absent) frames are excluded from BOTH training AND the recall denominator. So recall 31% = the model localizes only ~31% of touches **on the trace-VISIBLE frames it is actually trained/evaluated on** (a match = a decoded peak within `localization_tolerance=0.05` after greedy-NMS). It is NOT a "gate caps the denominator" artifact — the model genuinely fails to produce confident peaks at ~69% of the touches it DOES see. (Earlier "recall≈supervised-fraction 28.7%" match was coincidence.)
  - The trace-gate (`require_trace=True`, `trace_warm_threshold=200`) still matters as **training-data starvation**: 8203/11503 touch-frames get `label_mask=False` → the model trains on only ~29% of touch supervision. Ungating activity on trace-absent frames (supervise touch-present from the KNOWN command even without a visible trace; the agent-as-labeler premise) would ~3.5× the positive signal — a real lever, but for data quantity, not the eval ceiling.
  - Recall-limiting suspects, ranked to diagnose before spending on capacity: (a) **label precision / `latency_s` alignment** on the supervised set (XCTest offset never calibrated — flagged 2026-06-30; even trace-visible targets could be a few frames off → blurry targets → weak peaks); (b) **fully-autoregressive training** (this arm + Eslaf used teacher 0/0; val is autoregressive → compounding drift may suppress peaks); (c) **heatmap sigma / peak sharpness vs the 5% match tol**; (d) **capacity** (journal's 24/48ch follow-up). Capacity is LAST — a bigger model won't fix misaligned/blurry targets.
- GOAL (Asher, 2026-07-19): refine Model 1 to **90% strict**. Plan: (1) cheap diagnostics FIRST (no training): latency/on-trace sweep + eyeball supervised targets vs predicted heatmaps to see WHY peaks are weak; (2) act on whichever of alignment / ungating / teacher-schedule the diagnostics implicate; (3) capacity scaling only once labels+targets are clean. Honest caveat: 90% from 31% is a 3× recall jump — likely needs a labeling/target fix (and possibly more/better multi-touch data), not knob-tuning.

### Latency sweep — RULES OUT misalignment; trace is transient (2026-07-19)
- Ran the legacy `--sweep-latency` (CPU, cents) over 575 supercrown flick samples. Flick on-trace count (`trace_positives`) by latency: 0.10→299, 0.15→330, 0.20→351, **0.25→367 (peak)**, 0.30→333, 0.45→279. Spin positives rise monotonically to 0.45 (held-button profile, aligns later — a separate class).
- **Optimal flick latency ≈ 0.25** — only ~4% above the current 0.2. Adopt 0.25, but it is NOT the recall unlock.
- **Gating stays ~68% at EVERY latency** (never collapses at any offset) ⇒ the trace is genuinely TRANSIENT (renders during/just-after the flick then fades), not misaligned. So most touch-frames legitimately have no trace at the label. Latency hypothesis KILLED. Recall failure is on the on-trace supervised subset itself → next: VISUAL diagnostic (targets vs predicted heatmap peaks) to separate autoregressive drift / peak-sharpness / multi-touch. Then the ungating lever (supervise activity from the known command on trace-absent frames) is the leading data-side fix.

### Local per-frame diagnostic → heatmap head + a DATA-QUALITY BUG (2026-07-19)
Pulled 9 `sls_2015_super_crown` samples from `iPhone_XR2_20260712_024651`, ran checkpoint `..._133147` statefully on MPS (`scratchpad/diag_model1.py`). Findings:
- **Split of the failure: the ACTIVITY head works, the HEATMAP head doesn't.** Activity recall (touch-present detected, ignoring position) = **90%** (45/50). But peak recall = **0%**: the heatmap **global max caps at ~0.78–0.81**, never reaching the calibrated `peak_threshold=0.85` → **0 peaks decoded**. At thr=0.30 → 200 peaks (noisy). So the model knows WHEN a touch happens; its heatmap is too under-confident/diffuse to decode a peak at the strict threshold. ⇒ the lever is the heatmap head + peak calibration, NOT detection. Candidate cheap win: **activity-GATED peak decoding** (only decode where the 90%-accurate activity head fires) → allows a lower peak threshold to recover the ~0.6 bumps without the negative-frame noise that forced thr up to 0.85.
- **Warm-px @ 50 touch centers:** 76% have <50 warm-px, only 6% pass the ≥200 gate, 14% are 100–199 near-misses ⇒ lowering the gate threshold recovers little; the trace is truly absent for most touch-frames (confirms transience).
- **DATA-QUALITY BUG (undetected Bolt Challenges modal contamination).** Overlays showed the "BOLT CHALLENGES / COMPLETE THE STREAKS" **center modal** over gameplay, with the command GT touch on/near the modal and no trace. These frames carry **no `.menu`/`.editor` flag** AND `detect_menu_frames=True` skips **zero** of them — because `is_menu_frame` only inspects the **bottom 10% strip** (replay BACK/teal + app-hub cells), so a CENTER modal is invisible to it. ⇒ Bolt-modal frames leak into training with garbage command-position labels, teaching the heatmap head to hedge → under-confident peaks. Caveats: (1) this is ONE bad session — contamination is PARTIAL, not universal (full corpus still trained to 31%); (2) the "bump 0.6 at GT" read is on contaminated GT, so re-verify heatmap confidence on a CLEAN session.
- **NEXT:** (1) build a **center Bolt-modal detector** (dark rounded panel + red close-button signature), estimate corpus-wide contamination across sessions, reflag + exclude; (2) re-run this diagnostic on a CLEAN session to confirm the heatmap-confidence story; (3) then the training levers: activity-gated + lower peak threshold, sharper heatmap targets (lower `heatmap_sigma` / positive-peak loss), on cleaned data. Capacity still last.

### Modal detector VALIDATED + clean-data re-verify: heatmap head needs work, not just cleaning (2026-07-19)
- **Center-modal detector works.** Central-ROI `white_frac` = `(roi[0.18:0.82, 0.33:0.74] > 218).all(ch).mean()`: **0.612 on EVERY frame of the modal-stuck session** vs **0.000–0.016** across 12 samples from 4 other sessions. Clean bimodal separation; any threshold in [0.05,0.5] works. Entire session `iPhone_XR2_20260712_024651` was modal-stuck end-to-end and leaked wholesale into training — so contamination can be WHOLE-SESSION, not per-frame. Detector is cheap (numpy, one ROI) → wire into the collector guard AND the training loader/flagger.
- **Clean-data re-verify (12 clean samples, 9 retained seqs, 56 touch-frames):** heatmap global max median **0.796** (max 0.905), value AT GT center median **0.741** — real bump on the touch, but still caps ~0.8 < 0.85 threshold. Threshold sweep: peak=0.85 → recall 0; peak 0.30–0.70 → recall plateaus at **0.232** while **precision collapses to 0.04–0.05** (spurious peaks flood in); activity-gating (act 0.5) barely moved precision. strict-min stays ~0.05.
- **CONCLUSION: cleaning is necessary but NOT sufficient.** Even on clean data the heatmap head is simultaneously under-confident at true touches AND noisy elsewhere → no threshold separates them (the 31% knife-edge). The recall wall is a **heatmap TARGET/LOSS problem**: needs sharper targets (lower `heatmap_sigma`, currently 6.0 on 288×128) + much stronger background/hard-negative suppression to raise precision, retrained on modal-cleaned data. (Caveat: 9 clean seqs is small/noisy; the ~23% ≈ full-val 31%.)
- **Diagnosis-driven training arm (the real fix, vs 3 prior blind arms):** modal-clean the corpus → retrain with lower `heatmap_sigma` (~3–4) + higher `hard_negative_weight` for peak precision, keep the activity head. Capacity last. Implementing the modal detector in-code first (reusable by both loader + collector).
- **DONE: modal detector shipped to code + tested.** `gameplay_filter.bolt_modal_score()` / `is_bolt_modal_frame()` (central-ROI near-white fraction, `BOLT_MODAL_THRESH=0.15`). Validated via public API: 20/20 modal frames flagged, 0/20 clean false-positives, `is_menu_frame` catches 0/20 (complementary). 2 synthetic unit tests added; full suite **105 passed**. Wired into `TemporalTraceSequenceDataset` (folded into the `detect_menu_frames` non-gameplay check, own `detected_modal_skipped` counter) — verified the modal session goes 9 retained → 0 (`detected_modal_skipped=9`).

### Retrain arm #1 (modal-clean + hard-neg 0.20) → 34.78%, +3.3 (2026-07-19)
- `ap-XyMXTH6guaANUrj0XDanul`. Controlled A/B vs 31.5%: identical to prior arm EXCEPT modal-cleaning now active + hard-neg 0.05→0.20. Resume weights from Eslaf, lat 0.2, sigma 6.
- **Corpus contamination measured: `detected_modal_skipped=63`** (~3.5%; retained 1777→1714) — samples ALL prior runs silently trained on. Partial, as predicted.
- Best `..._160643` epoch 3: strict **34.78%** = recall 34.78%, **spec 90.78% (AT target!)**, peak-prec 36.15%; threshold dropped 0.85→0.75. Same peak-early(ep3)-then-fade to ~22% by ep40.
- **Read:** specificity is now solved; the wall is recall AND precision, both ~35% (heatmap peaks match ~35% of targets, ~35% of emitted peaks correct). Cleaning+precision gave the expected modest bump; the remaining lever is peak sharpness/confidence.
- Trajectory so far: 6.2 → 28.5 → 32.1 → 31.5 → **34.8**. Slowing in the mid-30s — 90% is far; may ultimately need more data (collection stopped) or arch change. NEXT: arm #2 from-scratch, `heatmap_sigma` 6→3.5 (sharper targets ⇒ more confident, better-localized peaks), keep hard-neg 0.20 + clean data. `heatmap_sigma` now plumbed through the harness.
- OPS: **SLS collection STOPPED** at Asher's request (sufficient corpus) — booted out `collect.xr1/xr2` + `watchdog.xr1/xr2` on the rig (rc=0, no orphans); `services`/`autooffload`/`dashboard`/remotexpc-tunnel left up; laptop `xrwatchdog` already unloaded.

### Retrain arm #2 (from-scratch, heatmap_sigma 3.5) → 25.86%, WORSE than arm #1 (2026-07-19)
- `ap-VBgZTwFpjKZPPo1qyQBfr4`. From-scratch (sigma≠6 breaks resume-compat), `heatmap_sigma` 6→**3.5** (sharper targets to attack peak under-confidence), scheduled teacher forcing 0.9→0.0 for cold start, kept arm-#1 wins (modal-clean + hard-neg 0.20), lat 0.2, 16/32ch, 40 epochs, cosine 1e-4→1e-5.
- Best `..._172512` **epoch 34, strict 25.86%** (recall 25.86%, spec 95.42%, peak-prec 52.00%, thr 0.85/0.80). Jagged curve, late peak, faded to ~20% by ep40. **WORSE than arm #1's 34.78%.**
- **Hypothesis FALSIFIED by direct experiment: sharper heatmap targets did NOT raise recall — they hurt it.** The under-confident-peak story does not respond to target sharpening; confounded partly by from-scratch cold-start, but the sign is unambiguous. Precision rose (52%) at recall's expense — the min-gate just moved.
- Trajectory now: 6.2 → 28.5 → 32.1 → 31.5 → **34.8** (arm #1, best) → 25.9 (arm #2, worse). **Five arms; plateaued low-mid 30s; the one target-quality lever I could test on existing data (sharpen) backfired.**
- **HONEST FEASIBILITY CALL:** every arm's val curve peaks by epoch 3–6 then fades = small-corpus overfitting; diagnostics show the heatmap head is under-confident at true touches AND noisy elsewhere with no separating threshold; the trace is genuinely transient so 71% of touch-frames have no visual cue at the label. **90% strict is not reachable by continued hyperparameter tuning on the current corpus.** The remaining grounded levers are: (A) **trace-ungating** — supervise touch position from the KNOWN command manifest even on trace-absent frames (agent-as-labeler; ~3.5× positive signal, uses existing data, directly attacks recall — but asks the model to localise touches with no visible trace, so ceiling unknown); (B) **more data** (collection was stopped at Asher's request); (C) accept ~35% operating point and proceed to Model 2. Capacity remains last (it already overfits). Escalated the fork to Asher rather than burning more A10G on blind tuning.

## Both-in-parallel toward ~99% Model 1: collection restart + Δ-calibration + UNGATING (2026-07-19)
Asher raised the bar to **~99% strict** (from 90%) before Model 2, and chose "both in parallel": resume collection (slow lever) AND work existing data (Δ-calibrate + ungate). Five prior tuning arms plateaued low-mid 30s; the val curves peak by epoch 3–6 then fade = data-limited overfitting.

### Rig collection RESTARTED (both phones)
- Reloaded launchd `collect.xr1/xr2` + `watchdog.xr1/xr2`. Both phones live, fresh sessions (seg 0+), SLS Super Crown, guard + all 3 contamination detectors (menu/hub, editor, Bolt-modal) active. remotexpc tunnel daemon up, 292 GiB free.
- **spin-frac 0.8 → 0.5** (Asher's call). Rationale: `--spin-frac` steers the CORPUS-WIDE spin fraction (all samples retained), currently only ~12% spin-family; 0.5 keeps raising it without going all-in. Do NOT reason about it as a per-session balance knob (memory `spin-frac-raises-corpus-wide-fraction`). Clean change: collectors were stopped, edited rig_collect.sh, relaunched fresh (no frozen-loop issue). Confirmed live args `--spin-frac 0.5` on both.

### Δ calibration (Modal sweep, current corpus, 1086 flick samples)
- Flick `trace_positives` by latency: 0.10→559, 0.15→580, **0.20→610**, **0.25→625 (peak)**, 0.30→585, 0.45→471. 0.20 is ~2.4% below the 0.25 peak.
- Spin `spin_positives`: monotone up 0.10→566 … **0.45→867** (held-button aligns much later — separate class).
- DECISION: **keep latency 0.2.** 0.25 is marginally better but breaks resume-compat (would force a from-scratch confound bigger than the 2.4% gain). The calibration's real value: 0.2 is near-optimal for the dominant flick class, and misalignment is NOT the recall wall (re-confirms the earlier sweep + the transience finding). Spin wants ~0.45 — a known single-scalar limitation, revisit with per-class latency only if the ungated arm shows spin-specific degradation.

### UNGATING arm #3 launched — `ap-a2r3qUJ7WilukkkFvSKv1S`
- **The agent-as-labeler unlock, no new code:** we issued every gesture, so the command manifest gives the finger (x,y) at every frame; the visible orange trace was only ever confirmation. `require_trace=False` stops hard-masking the ~71% trace-absent touch-frames and KEEPS their command-derived heatmap targets. Config: `--no-require-trace`, latency 0.2, else **identical to arm #1** (modal-clean data, resume-weights-only from Eslaf `..._121330`, hard-neg 0.20, heat/act 2.0/0.5, heat-pos 0.70, overlap 0.25, 16/32ch, batch 8, cosine 1e-4→1e-5, 40 epochs). Clean single-variable A/B vs arm #1's 34.78%.
- **Dataset confirms ungating took effect:** positive_frames **3248 → 11088 (3.4×)**, gated_frames **7903 → 0**, multi_touch_frames **146 → 624 (4.3×)**, multi_touch_sequences **63 → 171**. Same 1714 retained, modal-clean (63 skipped). Ungating recovers the trace-gated MULTI-TOUCH supervision too — directly attacks the starved case.
- **Metric caveat:** the ungated val recall denominator now includes trace-absent frames, so Modal-reported recall/strict is NOT directly comparable to arm #1. Compare **absolute matched touches** = pos%×N from the epoch logs (arm #1 ≈ 0.3478×~530 ≈ 184 matched); if the ungated arm matches MORE touches absolutely, it's localizing more even if the % looks lower. Result pending.

### Ungating arm #3 RESULT — REGRESSED via peak-flooding; killed at epoch 6 (2026-07-19)
- `ap-a2r3qUJ7WilukkkFvSKv1S`, stopped early (decisive by ep6). Full (hard) ungating: `require_trace=False`, else identical to arm #1.
- Epochs (N=1800 ungated targets vs arm #1's ~530): recall 2.2→7.9→9.1→10.4→**10.5%** (still climbing, not peaked); **matched touches ≈ 40→143→163→188→189** (barely edges arm #1's ~184 absolute). BUT **precision collapsed to ~3.8%** (plateaued by ep3), **emitting ~5000 peaks** (arm #1 ~500), and **negative-specificity crashed 90%→58%**. Decode threshold pinned low (0.20) to chase recall → spurious-peak flood.
- **VERDICT: full ungating is a net regression.** Exactly the predicted failure — forced to place sharp targets on the 71% of frames with NO visible trace, the heatmap head hedges: smears diffuse low-confidence mass everywhere → precision/specificity collapse. It does NOT learn to localise trace-absent touches; it learns to be uncertain. Small absolute recall gain not worth the selectivity collapse.
- **This exhausts the cheap existing-data lever.** Combined with the 5 tuning arms (plateau 25–35%), the evidence is now strong: **per-frame heatmap peak-localisation caps in the 30s on this self-labelled corpus** because the supervising signal (orange trace) is transient (only ~29% of touch-frames carry it) and the head is under-confident even on the trace-VISIBLE subset (clean-data diagnostic: caps ~0.8 < 0.85, floods at lower thr). 90–99% strict per-frame recall is not reachable by tuning or by ungating existing data.
- **Open reframe (unvalidated) — is per-frame recall the right target?** Model 2 consumes ASSEMBLED STROKES (build_bc_clips: per-frame peaks → causal touch tracks → `assemble_strokes`), which aggregate over a whole stroke's frame-window. A touch detected INTERMITTENTLY but consistently within each stroke window could yield high STROKE-LEVEL recovery even at ~35% per-frame recall. Worth measuring stroke-recovery fidelity of the current 34.78% Model 1 through the assembler BEFORE concluding Model 1 is inadequate — it may reframe "99% on Model 1" from a per-frame metric (likely unreachable) to a stroke-level metric (possibly already close).
- Levers remaining, honest ranking: (1) measure stroke-level recovery (cheap, could unblock Model 2 or confirm the gap); (2) more trace-VISIBLE data (collection restarted, slow — only helps if the visible-subset recall is itself data-limited); (3) soft/temporal-weighted ungating (weight trace-absent frames by proximity to a trace-visible anchor so temporal tracking is rewarded without forcing hedging — needs label_weight plumbing, marginal EV after this result); (4) reformulate Model 1 to predict stroke trajectories from clips rather than per-frame peaks. Capacity still last.

## Stroke-level recovery REFRAME MEASURED — does NOT rescue Model 1 (2026-07-19)
Asher approved measuring STROKE-level recovery (the metric Model 2 actually consumes) instead of per-frame recall, on the hypothesis that intermittent-but-consistent per-frame detection could still yield high stroke recovery. Built `stroke_recovery_remote` in the Modal harness (runs over the corpus volume; result persisted to /models to survive client blips): Model 1 → per-frame peaks → causal Hungarian tracking (`build_bc_clips.frames_to_touch_tracks`) → `touch_tracks_to_strokes` → match recovered vs command-manifest GT strokes on start+end ENDPOINT geometry (latency-independent). GT = drag `_TouchInterval` waypoints from `_schedule_from_meta`.
- **RESULT (arm #1 ckpt `..._160643` = best, 34.78% per-frame; 386 samples, 523 GT drag-strokes, peak_thresh 0.45):**
  - Stroke recall by endpoint tol: 0.05→**0.4%**, 0.10→3.4%, **0.12→4.8%**, 0.15→11.3%, 0.20(very loose)→29.1%.
  - Precision 0.1–6.7% — emits **2254 strokes for 523 real** (4.3× spurious flood).
  - Matched-stroke endpoint error mean **0.083** (and that's only the ~5% that matched; the rest are worse).
  - Per-kind recall @0.12 uniformly ~5% (flick 4.3, nslot 4.7, recipe 5.1, spin 5.0, spin_flick 8.0).
- **VERDICT: the reframe does NOT rescue Model 1 — it makes the picture WORSE, not better.** Stroke recall (~5% @ a reasonable 0.12 tol) is far below the ~35% per-frame recall. Aggregation/tracking **compounds** per-frame localisation noise rather than averaging it out: a stroke needs BOTH endpoints localised accurately AND tracked into one stroke AND not drowned by spurious strokes. At 34.78% per-frame with ~0.08 endpoint noise, that almost never happens.
- **Robustness:** no coordinate/aspect artifact (training targets and inference both live in the same 288×128 normalised-grid space, so distortion cancels; debug confirmed some strokes match tightly at 0.038 while most miss — genuine noise, not a systematic offset). Samples are mostly arm #1's TRAIN set, so leakage would only INFLATE recovery → the true held-out number is ≤ this. Lowering the peak threshold floods more strokes without raising recall (0.30–0.60 identical locally) → recall is localisation-limited, not decode-limited.
- **IMPLICATION — Asher's "~99% Model 1 before Model 2" instinct is VALIDATED.** At 35% per-frame, the assembled clip.json is ~5% real strokes / ~95% spurious — useless to train Model 2 on. There is no metric reinterpretation that unblocks Model 2; Model 1's per-frame localisation genuinely must improve a lot (tighter, more confident peaks on far more touch-frames). Real paths: (1) more trace-visible data (collection restarted, running), (2) higher-res heatmap / better target-loss for endpoint precision, (3) possibly reformulate Model 1 to regress stroke TRAJECTORIES from clips rather than per-frame peaks. The reframe is closed as a NEGATIVE result — valuable: it kills the "Model 2 may already be viable" hope and refocuses effort on Model 1 fidelity.
- Reusable tool shipped: `train_trace_extractor_modal.py --stroke-recovery --recovery-checkpoint <name> [--recovery-peak-threshold --recovery-tol]` → writes `/models/stroke_recovery_<ckpt>_pt<thr>.json`. Re-run on any future Model 1 to track stroke recovery as the north-star metric.
