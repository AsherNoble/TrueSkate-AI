# Repository guidance

Read `AGENTS.md` for authoritative instructions, `research/STATUS.md` for current
research, and `docs/WORKFLOWS.md` for commands. Behavioural cloning is current;
PPO/CMA-ES code is retired in tagged history. Run `python -m pytest`.

The research archive is append-only. Do not rewrite its existing entries.
